# ---- Core DL ----
# moshi requires torch<2.10; on Colab/servers use setup_colab.sh (installs matched
# wheels from the right CUDA index).
#
# Blackwell GPUs (RTX 50-series, RTX PRO 6000, B200 — sm_100/sm_120) need cu128
# wheels (torch>=2.7). The default PyPI / cu124 wheels only ship kernels up to
# sm_90 and crash at runtime with "no kernel image is available for execution on
# the device". On a Blackwell box install with:
#   pip install --index-url https://download.pytorch.org/whl/cu128 \
#       "torch>=2.8,<2.10" "torchaudio>=2.8,<2.10"
torch>=2.2.0,<2.10
torchaudio>=2.2.0,<2.10
numpy>=1.24
einops>=0.7.0

# ---- Audio codec (Mimi) + Kyutai stack ----
moshi>=0.2.0

# ---- Text tokenization ----
transformers>=4.40.0
sentencepiece>=0.2.0
tokenizers>=0.19.0

# ---- Data ----
datasets>=2.19.0
huggingface_hub>=0.23.0
soundfile>=0.12.1
librosa>=0.10.0

# ---- Experiment tracking ----
wandb>=0.17.0

# ---- Utilities ----
tqdm>=4.66.0
pyyaml>=6.0
rich>=13.0
